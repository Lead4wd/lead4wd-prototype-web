"use client";

import LessonView from "@/components/views/LessonView";

/** `/lesson` — whichever module the journey says is next. */
export default function LessonPage() {
  return <LessonView />;
}

{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts",
    "**/*.mts"
  ],
  "_comment_exclude": [
    "vitest.config.ts is build tooling, not app code. It has to stay out of the",
    "Next typecheck: Next 16 bundles a rolldown-based Vite while Vitest bundles",
    "its own, so the two copies' Plugin types are structurally incompatible and",
    "`plugins: [react()]` fails to typecheck even though it runs fine.",
    "Vitest reads this file directly and does not typecheck it. The tests",
    "themselves stay included, so `npm run build` still checks them."
  ],
  "exclude": ["node_modules", "vitest.config.ts"]
}
